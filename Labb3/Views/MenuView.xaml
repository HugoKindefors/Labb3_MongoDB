<UserControl x:Class="Labb3.Views.MenuView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:fa="http://schemas.awesome.incremented/wpf/xaml/fontawesome.sharp"
             xmlns:local="clr-namespace:Labb3.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <UserControl.DataContext>
        <Binding RelativeSource="{RelativeSource AncestorType=Window}" Path="DataContext"/>
    </UserControl.DataContext>
    <Menu>
        <MenuItem Header="_File">
            <MenuItem Header="_New Question Pack" Icon="{fa:Icon folderplus}" Command="{Binding NewPackCommand}" InputGestureText="Ctrl+N"/>
            <MenuItem Header="_Select Question Pack" Icon="{fa:Icon folder}" InputGestureText="Ctrl+O">
                <MenuItem.ItemsSource>
                    <Binding Path="Packs"/>
                </MenuItem.ItemsSource>
                <MenuItem.ItemTemplate>
                    <DataTemplate>
                        <MenuItem Header="{Binding Name}" 
                                  Command="{Binding DataContext.SelectPackCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                  CommandParameter="{Binding}"/>
                    </DataTemplate>
                </MenuItem.ItemTemplate>
            </MenuItem>
            <MenuItem Header="Save _Pack" Icon="{fa:Icon floppydisk}" Command="{Binding SavePackCommand}" InputGestureText="Ctrl+S"/>
            <Separator/>
            <MenuItem Header="_Delete Question Pack" Icon="{fa:Icon folderminus}" Command="{Binding DeletePackCommand}" InputGestureText="Ctrl+Delete"/>
            <Separator/>
            <MenuItem Header="Import Question Pack" Icon="{fa:Icon cloudarrowup}"/>
            <Separator/>
            <MenuItem Header="E_xit" InputGestureText="Alt+F4" Icon="{fa:Icon dooropen}"/>
        </MenuItem>
        <MenuItem Header="_Edit">
            <MenuItem Header="Add Question" Icon="{fa:Icon plus}" Command="{Binding ConfigurationViewModel.AddQuestionCommand}" InputGestureText="Ctrl+A"/>
            <MenuItem Header="Remove Question" Icon ="{fa:Icon minus}" Command="{Binding ConfigurationViewModel.RemoveQuestionCommand}" InputGestureText="Alt+Q" />
            <MenuItem Header="Pack Options"  Icon="{fa:Icon sliders }" Command="{Binding OpenPackOptionsCommand}" InputGestureText="Ctrl+P"/>
        </MenuItem>
        <MenuItem Header="_View" >
            <MenuItem Header="Play" Icon="{fa:Icon play}" Command="{Binding StartPlayCommand}" InputGestureText="Alt+P"/>
            <MenuItem Header="Edit" Icon="{fa:Icon gears }" Command="{Binding SwitchToConfigurationCommand}" InputGestureText="Ctrl+E"/>
            <MenuItem Header="Fullscreen" Icon="{fa:Icon expand}" Command="{Binding ToggleFullScreenCommand}" InputGestureText="Alt+F11"/>
        </MenuItem>
    </Menu>
</UserControl>
